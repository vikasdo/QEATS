version 1 works great.

version 2 does not allow both upload and download.

version 3 does not allow download of files.

version 4,5,6,7 works fine but version 4 takes a bit more time than other three.